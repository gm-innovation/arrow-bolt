import CorpLayout from '@/components/corp/CorpLayout';

const CorpRequests = () => {
  return (
    <CorpLayout>
      <div className="text-center py-12 text-muted-foreground">
        <h2 className="text-xl font-semibold mb-2">Requisições</h2>
        <p>Página será implementada na Etapa 3.</p>
      </div>
    </CorpLayout>
  );
};

export default CorpRequests;
