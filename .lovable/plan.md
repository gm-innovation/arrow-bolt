Em `src/pages/careers/PublicCareers.tsx`, aumentar as classes de altura das logos em ~50%:

- Nav (hero e listing): `h-10 md:h-12` → `h-14 md:h-16`
- Hero (logo cinza acima do badge): altura atual ~h-10 → `h-16`
- Footer: `h-6` → `h-9`

Apenas ajuste de tamanho; nada mais muda.